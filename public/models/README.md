# MediaPipe Model

Place the MediaPipe Hand Landmarker model at:

```text
public/models/hand_landmarker.task
```

The app does not commit the model binary by default. Download the official model from Google AI Edge / MediaPipe documentation and verify its license before redistribution.

The camera practice page will show a clear model-loading error until this file is present.
